<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.kusitms.kbscilpoon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Fragment.Activity.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Fragment.Activity.IntroduceActivity"
            android:theme="@style/AppTheme.NoTitleBar" />
        <activity
            android:name=".Fragment.Activity.MainActivity"
            android:theme="@style/AppTheme.NoTitleBar" />
        <activity android:name=".Chatbot.Activity.ChatbotActivity" />
        <activity android:name=".Fragment.Activity.MateActivity" />
        <activity android:name=".Fragment.Activity.ChatInfoActivity" />
        <activity android:name=".Fragment.Activity.ChatInfo2Activity" />
        <activity android:name=".Fragment.Activity.ChatInfo3Activity"></activity>
    </application>

</manifest>