<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.kusitms.kbscilpoon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".IntroduceActivity"
            android:theme="@style/AppTheme.NoTitleBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoTitleBar" />

        <activity android:name=".Chatbot.Activity.ChatbotActivity"></activity>
        <activity android:name=".Chatbot.Activity.Chatbot2Activity"></activity>
        <activity android:name=".Fragment.Activity.MateActivity"></activity>
        <activity android:name=".Fragment.Activity.InforActivity"> </activity>

        <activity
            android:name=".Chatbot.Activity.CardDetail1"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".Chatbot.Activity.CardDetail2"
            android:theme="@android:style/Theme.Dialog" />
        <activity android:name=".Chatbot.Activity.CardDetail3"
            android:theme="@android:style/Theme.Dialog" ></activity>

    </application>

</manifest>